'use client';
import { useState } from 'react';
import { CheckCircle2, Clock3, PackageCheck, Truck, XCircle } from 'lucide-react';

type Order = { order_number: string; customer_name: string; customer_phone: string; shipping_address: string; items_json: string; total: number; status: string; created_at: string };
const labels: Record<string,string> = { menunggu_pembayaran:'Menunggu transfer', dibayar:'Sudah dibayar', diproses:'Diproses', dikirim:'Dikirim', selesai:'Selesai', dibatalkan:'Dibatalkan' };
const icons: Record<string,typeof Clock3> = { menunggu_pembayaran:Clock3, dibayar:CheckCircle2, diproses:PackageCheck, dikirim:Truck, selesai:CheckCircle2, dibatalkan:XCircle };
const rupiah = (v:number) => new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v);

export function AdminSetup() { const [code,setCode]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(){setBusy(true);setError('');const r=await fetch('/api/admin/setup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})});if(r.ok) location.reload();else setError((await r.json()).error ?? 'Gagal mengaktifkan.');setBusy(false)} return <div className="mx-auto mt-20 max-w-md rounded-3xl border bg-white p-8 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#a34f2c]">Aktivasi pertama</p><h1 className="mt-3 font-serif text-3xl">Hubungkan akun admin</h1><p className="mt-3 text-sm leading-6 text-[#68736b]">Masukkan kode aktivasi satu kali. Setelah berhasil, hanya akun ChatGPT ini yang dapat membuka panel admin.</p><input value={code} onChange={e=>setCode(e.target.value)} type="password" placeholder="Kode aktivasi" className="mt-6 w-full rounded-xl border px-4 py-3 outline-none focus:border-[#243b2c]"/><button onClick={submit} disabled={busy||!code} className="mt-3 w-full rounded-full bg-[#243b2c] py-3 font-semibold text-white disabled:opacity-50">{busy?'Mengaktifkan…':'Aktifkan admin'}</button>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}</div> }

export function AdminDashboard({ initialOrders, adminName }: { initialOrders: Order[]; adminName: string }) { const [orders,setOrders]=useState(initialOrders); const [filter,setFilter]=useState('semua'); const visible=filter==='semua'?orders:orders.filter(o=>o.status===filter); async function update(orderNumber:string,status:string){const r=await fetch('/api/admin/orders',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({orderNumber,status})});if(r.ok)setOrders(os=>os.map(o=>o.order_number===orderNumber?{...o,status}:o));} const open=orders.filter(o=>!['selesai','dibatalkan'].includes(o.status)).length; const revenue=orders.filter(o=>['dibayar','diproses','dikirim','selesai'].includes(o.status)).reduce((s,o)=>s+o.total,0); return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><header className="flex flex-col justify-between gap-5 border-b pb-7 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#a34f2c]">Simple Ground Admin</p><h1 className="mt-2 font-serif text-4xl">Ringkasan pesanan</h1><p className="mt-2 text-sm text-[#68736b]">Halo, {adminName}</p></div><div className="flex gap-2"><a href="/" className="rounded-full border px-4 py-2 text-sm font-semibold">Lihat toko</a><a href="/signout-with-chatgpt?return_to=/" className="rounded-full bg-[#243b2c] px-4 py-2 text-sm font-semibold text-white">Keluar</a></div></header><div className="mt-7 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-[#243b2c] p-5 text-white"><p className="text-xs text-white/70">Perlu ditangani</p><b className="mt-2 block font-serif text-4xl">{open}</b></div><div className="rounded-2xl bg-white p-5"><p className="text-xs text-[#68736b]">Total pesanan</p><b className="mt-2 block font-serif text-4xl">{orders.length}</b></div><div className="rounded-2xl bg-[#efe7d8] p-5"><p className="text-xs text-[#68736b]">Penjualan terkonfirmasi</p><b className="mt-2 block font-serif text-2xl">{rupiah(revenue)}</b></div></div><div className="mt-7 flex gap-2 overflow-auto pb-2">{['semua',...Object.keys(labels)].map(s=><button key={s} onClick={()=>setFilter(s)} className={`whitespace-nowrap rounded-full border px-4 py-2 text-sm ${filter===s?'bg-[#243b2c] text-white':'bg-white'}`}>{s==='semua'?'Semua':labels[s]}</button>)}</div><div className="mt-4 space-y-4">{visible.length===0?<div className="rounded-2xl border border-dashed p-12 text-center text-[#68736b]">Belum ada pesanan pada status ini.</div>:visible.map(o=>{const Icon=icons[o.status]??Clock3;const items=JSON.parse(o.items_json) as {name:string;quantity:number}[];return <article key={o.order_number} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><div className="flex items-center gap-2"><Icon size={17}/><b>{o.order_number}</b><span className="rounded-full bg-[#edf1e9] px-2.5 py-1 text-[11px] font-semibold">{labels[o.status]}</span></div><p className="mt-2 text-sm font-semibold">{o.customer_name} · <a className="underline" href={`https://wa.me/${o.customer_phone.replace(/\D/g,'').replace(/^0/,'62')}`}>{o.customer_phone}</a></p><p className="mt-1 max-w-xl text-xs leading-5 text-[#68736b]">{o.shipping_address}</p></div><div className="sm:text-right"><b className="text-lg">{rupiah(o.total)}</b><p className="mt-1 text-xs text-[#68736b]">{new Date(o.created_at).toLocaleString('id-ID')}</p></div></div><div className="mt-4 border-t pt-4"><p className="text-sm">{items.map(i=>`${i.quantity}× ${i.name}`).join(' · ')}</p><select value={o.status} onChange={e=>update(o.order_number,e.target.value)} className="mt-4 rounded-xl border bg-[#f7f4ec] px-3 py-2 text-sm font-semibold">{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div></article>})}</div></div> }
