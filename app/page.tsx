'use client';

import { useMemo, useState } from 'react';
import { ArrowRight, Check, ChevronLeft, Minus, Plus, Search, ShoppingBag, X } from 'lucide-react';

const products = [
  { id: 1, name: 'Kemeja Linen Daily', category: 'Daily', price: 289000, image: 'https://images.unsplash.com/photo-1598033129183-c4f50c736f10?auto=format&fit=crop&w=900&q=85', tone: 'Sand' },
  { id: 2, name: 'Kaos Daily Essential', category: 'Daily', price: 159000, image: 'https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=900&q=85', tone: 'Oat' },
  { id: 3, name: 'Celana Linen Relaxed', category: 'Daily', price: 319000, image: 'https://images.unsplash.com/photo-1506629082955-511b1aa562c8?auto=format&fit=crop&w=900&q=85', tone: 'Sage' },
  { id: 4, name: 'Baju Chef Signature', category: 'Chef', price: 349000, image: 'https://images.unsplash.com/photo-1577219491135-ce391730fb2c?auto=format&fit=crop&w=900&q=85', tone: 'White' },
  { id: 5, name: 'Apron Canvas Ground', category: 'Chef', price: 219000, image: 'https://images.unsplash.com/photo-1556911220-bff31c812dba?auto=format&fit=crop&w=900&q=85', tone: 'Earth' },
  { id: 6, name: 'Topi Chef Classic', category: 'Chef', price: 129000, image: 'https://images.unsplash.com/photo-1577106263724-2c8e03bfe9cf?auto=format&fit=crop&w=900&q=85', tone: 'White' },
];
const rupiah = (value: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(value);

export default function Home() {
  const [category, setCategory] = useState('Semua');
  const [query, setQuery] = useState('');
  const [cart, setCart] = useState<Record<number, number>>({});
  const [cartOpen, setCartOpen] = useState(false);
  const [checkout, setCheckout] = useState(false);
  const [ordered, setOrdered] = useState(false);
  const filtered = useMemo(() => products.filter((p) => (category === 'Semua' || p.category === category) && p.name.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const count = Object.values(cart).reduce((a, b) => a + b, 0);
  const subtotal = products.reduce((sum, p) => sum + p.price * (cart[p.id] || 0), 0);
  const shipping = subtotal ? 18000 : 0;
  function changeItem(id: number, delta: number) { setCart((current) => { const next = Math.max(0, (current[id] || 0) + delta); const result = { ...current, [id]: next }; if (!next) delete result[id]; return result; }); }

  return <main className="min-h-screen bg-[#f7f4ec] text-[#1f2b22]">
    <div className="bg-[#243b2c] px-4 py-2 text-center text-[11px] font-semibold tracking-[.18em] text-[#f7f1df] sm:text-xs">GRATIS ONGKIR UNTUK PEMBELIAN DI ATAS RP500.000</div>
    <header className="sticky top-0 z-30 border-b border-[#263e2e]/10 bg-[#f7f4ec]/95 backdrop-blur"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 sm:px-8">
      <a href="#home" className="font-serif text-2xl font-bold tracking-[-.04em]">simple ground<span className="text-[#c0693c]">.</span></a>
      <nav className="hidden items-center gap-8 text-sm font-medium md:flex"><a href="#koleksi">Koleksi</a><a href="#cerita">Cerita Kami</a><a href="#footer">Bantuan</a></nav>
      <div className="flex items-center gap-2"><label className="hidden items-center gap-2 rounded-full border border-[#263e2e]/15 bg-white px-4 py-2 lg:flex"><Search size={16}/><input value={query} onChange={(e) => setQuery(e.target.value)} className="w-32 bg-transparent text-sm outline-none" placeholder="Cari produk" /></label><button onClick={() => setCartOpen(true)} aria-label={`Buka keranjang, ${count} barang`} className="relative grid h-10 w-10 place-items-center rounded-full hover:bg-[#e8e2d3]"><ShoppingBag size={21}/>{count > 0 && <span className="absolute right-0 top-0 grid h-5 min-w-5 place-items-center rounded-full bg-[#c0693c] px-1 text-[10px] font-bold text-white">{count}</span>}</button></div>
    </div></header>

    <section id="home" className="mx-auto grid max-w-7xl gap-8 px-5 pb-20 pt-10 sm:px-8 lg:grid-cols-[1.02fr_.98fr] lg:items-center lg:pt-16">
      <div className="order-2 max-w-xl lg:order-1"><p className="mb-5 text-xs font-bold uppercase tracking-[.24em] text-[#a34f2c]">Hidup lebih ringan</p><h1 className="font-serif text-5xl leading-[.98] tracking-[-.055em] sm:text-7xl">Yang esensial,<br/><em className="font-normal text-[#657760]">dibuat berarti.</em></h1><p className="mt-7 max-w-md text-base leading-7 text-[#536056]">Pakaian dan benda keseharian yang jujur, nyaman, dan dibuat untuk bertahan lebih lama.</p><a href="#koleksi" className="mt-8 inline-flex items-center gap-3 rounded-full bg-[#243b2c] px-6 py-3.5 text-sm font-semibold text-white transition hover:-translate-y-0.5 hover:bg-[#c0693c]">Belanja koleksi <ArrowRight size={17}/></a><div className="mt-10 flex gap-8 border-t border-[#263e2e]/15 pt-5 text-xs text-[#536056]"><span>Material alami</span><span>Produksi terbatas</span><span>Dibuat di Indonesia</span></div></div>
      <div className="order-1 relative lg:order-2"><div className="aspect-[4/5] overflow-hidden rounded-[2.25rem] bg-[#d9d1bf]"><img src="https://images.unsplash.com/photo-1483985988355-763728e1935b?auto=format&fit=crop&w=1200&q=90" alt="Koleksi busana bernuansa natural Simple Ground" className="h-full w-full object-cover" /></div><div className="absolute -bottom-5 -left-3 rounded-2xl bg-[#f7f4ec] p-3 shadow-xl sm:-left-7 sm:p-4"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#a34f2c]">Baru hadir</p><p className="mt-1 font-serif text-lg">The Ground Edit</p></div></div>
    </section>

    <section id="koleksi" className="bg-[#fffdf8] px-5 py-20 sm:px-8"><div className="mx-auto max-w-7xl">
      <div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-[#a34f2c]">Pilihan kami</p><h2 className="mt-3 font-serif text-4xl tracking-[-.04em] sm:text-5xl">Daily & kitchen wear.</h2></div><div className="flex flex-wrap gap-2">{['Semua','Daily','Chef'].map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-full border px-4 py-2 text-sm ${category === item ? 'border-[#243b2c] bg-[#243b2c] text-white' : 'border-[#263e2e]/15'}`}>{item}</button>)}</div></div>
      <div className="mt-10 grid gap-x-5 gap-y-10 sm:grid-cols-2 lg:grid-cols-4">{filtered.map((p) => <article key={p.id} className="group"><div className="relative aspect-[4/5] overflow-hidden rounded-2xl bg-[#ebe5d9]"><img src={p.image} alt={p.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.04]"/><span className="absolute left-3 top-3 rounded-full bg-[#fffdf8]/90 px-3 py-1 text-[10px] font-bold uppercase tracking-wider">{p.tone}</span><button onClick={() => { changeItem(p.id, 1); setCartOpen(true); }} className="absolute bottom-3 left-3 right-3 translate-y-2 rounded-full bg-[#243b2c] py-3 text-sm font-semibold text-white opacity-0 transition group-hover:translate-y-0 group-hover:opacity-100 focus:translate-y-0 focus:opacity-100">Tambah ke keranjang</button></div><div className="mt-4 flex items-start justify-between gap-3"><div><h3 className="font-semibold">{p.name}</h3><p className="mt-1 text-xs text-[#758078]">{p.category}</p></div><p className="whitespace-nowrap text-sm font-semibold">{rupiah(p.price)}</p></div><button onClick={() => { changeItem(p.id, 1); setCartOpen(true); }} className="mt-3 w-full rounded-full border border-[#263e2e]/20 py-2.5 text-sm font-semibold sm:hidden">+ Keranjang</button></article>)}</div>
    </div></section>

    <section id="cerita" className="mx-auto grid max-w-7xl gap-8 px-5 py-20 sm:px-8 lg:grid-cols-2 lg:items-center"><div className="overflow-hidden rounded-[2rem]"><img src="https://images.unsplash.com/photo-1529139574466-a303027c1d8b?auto=format&fit=crop&w=1100&q=85" alt="Detail kain dan proses produksi yang teliti" className="aspect-[4/3] w-full object-cover"/></div><div className="lg:pl-14"><p className="text-xs font-bold uppercase tracking-[.22em] text-[#a34f2c]">Tentang Simple Ground</p><h2 className="mt-4 font-serif text-4xl leading-tight tracking-[-.04em]">Lebih sedikit, lebih dekat dengan yang penting.</h2><p className="mt-6 leading-7 text-[#566158]">Kami percaya benda yang baik tidak perlu berteriak. Setiap koleksi dirancang dalam jumlah terbatas, mengutamakan bahan nyaman dan siluet yang mudah dipakai berulang kali.</p><div className="mt-8 grid grid-cols-3 gap-4 border-t border-[#263e2e]/15 pt-6"><div><b className="font-serif text-3xl">100%</b><p className="mt-1 text-xs text-[#68736b]">material pilihan</p></div><div><b className="font-serif text-3xl">30 hari</b><p className="mt-1 text-xs text-[#68736b]">tukar ukuran</p></div><div><b className="font-serif text-3xl">Lokal</b><p className="mt-1 text-xs text-[#68736b]">produksi etis</p></div></div></div></section>

    <footer id="footer" className="bg-[#243b2c] px-5 py-14 text-[#f4efdf] sm:px-8"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-[1.5fr_1fr_1fr]"><div><p className="font-serif text-3xl font-bold">simple ground.</p><p className="mt-4 max-w-sm text-sm leading-6 text-[#c6d0c6]">Daily wear dan kitchen wear yang sederhana, nyaman, dan tahan lama.</p></div><div><p className="text-xs font-bold uppercase tracking-[.18em]">Belanja</p><div className="mt-4 space-y-3 text-sm text-[#c6d0c6]"><p>Koleksi Daily</p><p>Baju Chef</p><p>Apron & Topi Chef</p></div></div><div><p className="text-xs font-bold uppercase tracking-[.18em]">Bantuan</p><div className="mt-4 space-y-3 text-sm text-[#c6d0c6]"><p>Pengiriman & retur</p><p>Konfirmasi pembayaran</p><a href="https://wa.me/6285172381996" className="block">WhatsApp: 0851-7238-1996</a></div></div></div><p className="mx-auto mt-12 max-w-7xl border-t border-white/15 pt-5 text-xs text-[#9eae9f]">© 2026 Simple Ground. Dibuat dengan perhatian.</p></footer>

    {cartOpen && <div className="fixed inset-0 z-50 bg-black/35" onMouseDown={(e) => e.target === e.currentTarget && setCartOpen(false)}><aside className="ml-auto flex h-full w-full max-w-md flex-col bg-[#fffdf8] shadow-2xl"><div className="flex items-center justify-between border-b border-[#263e2e]/10 p-5"><div>{checkout && <button onClick={() => { setCheckout(false); setOrdered(false); }} className="mr-3 align-middle"><ChevronLeft size={20}/></button>}<b className="font-serif text-2xl">{checkout ? 'Pembayaran' : 'Keranjang'} {(!checkout && count > 0) && `(${count})`}</b></div><button onClick={() => setCartOpen(false)} aria-label="Tutup"><X/></button></div>
      {!checkout ? <><div className="flex-1 overflow-auto p-5">{count === 0 ? <div className="grid h-full place-content-center text-center"><ShoppingBag className="mx-auto mb-4 text-[#7b877e]" size={38}/><p className="font-serif text-xl">Keranjangmu masih kosong.</p><button onClick={() => setCartOpen(false)} className="mt-4 text-sm font-semibold underline">Lihat koleksi</button></div> : <div className="space-y-5">{products.filter((p) => cart[p.id]).map((p) => <div key={p.id} className="flex gap-4"><img src={p.image} alt="" className="h-24 w-20 rounded-xl object-cover"/><div className="flex flex-1 flex-col"><b className="text-sm">{p.name}</b><span className="mt-1 text-xs text-[#758078]">{p.tone}</span><div className="mt-auto flex items-center justify-between"><div className="flex items-center gap-3 rounded-full border px-2 py-1"><button onClick={() => changeItem(p.id,-1)} aria-label="Kurangi"><Minus size={13}/></button><span className="text-sm">{cart[p.id]}</span><button onClick={() => changeItem(p.id,1)} aria-label="Tambah"><Plus size={13}/></button></div><b className="text-sm">{rupiah(p.price * cart[p.id])}</b></div></div></div>)}</div>}</div>{count > 0 && <div className="border-t p-5"><div className="flex justify-between text-sm"><span>Subtotal</span><b>{rupiah(subtotal)}</b></div><p className="mt-2 text-xs text-[#758078]">Ongkir dihitung saat checkout.</p><button onClick={() => setCheckout(true)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-full bg-[#243b2c] py-3.5 font-semibold text-white">Lanjut checkout <ArrowRight size={17}/></button></div>}</> : ordered ? <div className="grid flex-1 place-content-center overflow-auto p-8 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-[#dce8dc] text-[#243b2c]"><Check size={28}/></span><h3 className="mt-5 font-serif text-3xl">Pesanan diterima!</h3><p className="mt-3 text-sm leading-6 text-[#637067]">Nomor pesanan <b>SG-260831</b>. Silakan selesaikan transfer.</p><div className="mt-6 rounded-2xl bg-[#f1ecdf] p-5 text-left text-sm"><p className="text-xs text-[#758078]">Transfer Bank Mandiri</p><p className="mt-1 text-lg font-bold">9000027694984</p><p className="text-xs">a.n. Muhammad Arifin</p><div className="my-4 border-t border-[#263e2e]/10"/><p className="text-xs text-[#758078]">Total transfer</p><p className="mt-1 text-lg font-bold text-[#a34f2c]">{rupiah(subtotal + shipping)}</p></div><a href={`https://wa.me/6285172381996?text=${encodeURIComponent(`Halo Simple Ground, saya ingin konfirmasi pembayaran pesanan SG-260831 sebesar ${rupiah(subtotal + shipping)}.`)}`} className="mt-4 rounded-full bg-[#243b2c] px-5 py-3 text-sm font-semibold text-white">Konfirmasi via WhatsApp</a><p className="mt-3 text-xs leading-5 text-[#758078]">Bayar dalam 24 jam agar pesanan tetap tersimpan.</p></div> : <div className="flex-1 overflow-auto p-5"><div><label className="text-xs font-bold uppercase tracking-wider">Nama lengkap</label><input className="mt-2 w-full rounded-xl border bg-white px-4 py-3 outline-none" placeholder="Nama penerima"/></div><div className="mt-4"><label className="text-xs font-bold uppercase tracking-wider">Alamat pengiriman</label><textarea className="mt-2 min-h-24 w-full rounded-xl border bg-white px-4 py-3 outline-none" placeholder="Jalan, kecamatan, kota, kode pos"/></div><div className="mt-6"><p className="text-xs font-bold uppercase tracking-wider">Pembayaran</p><div className="mt-2 rounded-2xl border border-[#243b2c] bg-[#edf1e9] p-4"><b>Bank Mandiri</b><p className="mt-1 text-sm">9000027694984 · Muhammad Arifin</p></div></div><div className="mt-6 rounded-2xl bg-[#f1ecdf] p-4 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{rupiah(subtotal)}</span></div><div className="mt-2 flex justify-between"><span>Pengiriman</span><span>{rupiah(shipping)}</span></div><div className="mt-3 flex justify-between border-t pt-3 font-bold"><span>Total</span><span>{rupiah(subtotal + shipping)}</span></div></div><button onClick={() => setOrdered(true)} className="mt-5 w-full rounded-full bg-[#c0693c] py-3.5 font-semibold text-white">Buat pesanan & lihat rekening</button><p className="mt-3 text-center text-[11px] leading-4 text-[#758078]">Stok akan ditahan selama 24 jam.</p></div>}
    </aside></div>}
  </main>;
}
